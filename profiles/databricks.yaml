# [Recommended] Uncomment fields below to set an MLflow experiment to track the pipeline execution.
# If unset, a default experiment based on runtime context will be created.
# experiment:
  # name: "/Shared/sklearn_regression_experiment"

INGEST_DATA_LOCATION: "feature_store_taxi_example.nyc_yellow_taxi_with_zips"
# Set the INGEST_DATA_FORMAT corresponding to the data_set to correctly ingest the data.
INGEST_DATA_FORMAT: spark_sql
# Override the default split ratio by changing the Train/validation/test ratio respectively
# SPLIT_RATIOS: [0.75, 0.125, 0.125]
